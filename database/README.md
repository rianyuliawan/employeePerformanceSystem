# Database

Folder ini berisi struktur PostgreSQL untuk data operasional sistem.

- `schema/init.sql`: schema awal yang bisa dijalankan otomatis oleh Docker Compose.
- `migration`: tempat migration SQL tambahan.
- `seed`: tempat data awal.
- `backup`: tempat backup database.

